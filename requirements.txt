pyyaml>=6
pytest>=8
# optional, for later work:
# ccxt>=4
# pandas>=2
# numpy>=1.26
tzdata ; sys_platform == "win32"   # zoneinfo has no tz database on Windows; bin/study-hours falls back to the EU rule without it
