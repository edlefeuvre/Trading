# common/exchange

Shared. Depended on by: TS01.
Behavioural changes here require a change-log row in every dependent STRATEGY.md
(the pre-commit hook enforces this for engine/ and exchange/).

Migration target — see common/README.md.
